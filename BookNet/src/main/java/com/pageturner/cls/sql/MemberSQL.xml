<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
			PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
					"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
					
<mapper namespace="mSQL">
	<sql id="selIdCNT">
		SELECT
			count(*) cnt
		FROM
			membertab
		WHERE
			id = 
	</sql>
	
	<!-- 로그인처리를 위한 sql 질의명령 -->
	<select id="Login" resultType="int" parameterType="mVO">
		<include refid="selIdCNT" /> #{id}
			AND pw = #{pw}
	</select>
	
	<!-- 정보수정(pw) -->
	<update id="editPW" parameterType="mVO">
		update
			membertab
		set
			pw = #{pw}
		where
			id = #{id}
	</update>
	
	<!-- 정보수정 -->
	<update id="editUser" parameterType="mVO">
		update
			membertab
		set
			interest = #{interest}
			describe = #{describe}
			intershow = #{intershow}
			birthshow = #{birthshow}
			genshow = #{genshow}
		where
			id = #{id}
	</update>
	
	<!-- 정보수정(프사추가) -->
	<insert id="insertProfile" parameterType="fVO">
		insert into
			profilepictab(pf_pt_no, mno, ori_name, save_name, save_loc)
		values(
			(select nvl(max(pf_pt_no)+1, 1000) from profilepictab),
			(select mno from membertab where id = #{id}),
			#{ori_name}, #{save_name}, #{save_loc}
		)
	</insert>
	
	<!-- 회원탈퇴 -->
	<update id="delUser" parameterType="mVO">
		update
			membertab
		set
			isshow = N
		where
			id = #{id}
	</update>
	
	<!-- 아디비번찾기 -->
	<select id="findID" resultType="String" parameterType="mVO">
		select
			id
		from
			member
		where
			mail = #{mail}
	</select>
	<select id="findPW" resultType="String" parameterType="mVO">
		select
			pw
		from
			member
		where
			id = #{id}
			and mail = #{mail}
	</select>
	<!-- 아이디 중복 확인(우현우)  -->
	<select id="idck" resultType="int" parameterType="mVO">
		<include refid="selIdCNT"/>#{id}	
	</select>
	
	<!-- 회원가입 처리(우현우) -->
	<insert id="join"  parameterType="mVO">
		<selectKey keyProperty="mno" resultType="int" order="BEFORE">
			SELECT 
				NVL(MAX(mno) + 1, 1000)
			FROM
				membertab
		</selectKey>
		insert into
			membertab(mno, id, pw, name, mail, tel, gen, birthdate, joindate, describe)
		VALUES(
			#{id}, #{pw}, #{name}, #{mail}, #{tel}, #{gen}, #{birthdate}, sysdate, #{describe}
		)	
	</insert>
</mapper>					